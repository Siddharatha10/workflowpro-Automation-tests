[pytest]
markers =
    smoke: fast subset run on every PR
    regression: full suite run nightly
    mobile: tests requiring a mobile viewport / real device
addopts = -v --html=reports/report.html --self-contained-html --junitxml=reports/junit.xml
